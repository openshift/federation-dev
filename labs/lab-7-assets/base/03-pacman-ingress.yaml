apiVersion: extensions/v1beta1
kind: Ingress
metadata:
  name: pacman 
  namespace: pacman
spec:
  rules:
  - host: pacmanhosthere
    http:
      paths:
      - path: /
        backend:
          serviceName: pacman 
          servicePort: 80
