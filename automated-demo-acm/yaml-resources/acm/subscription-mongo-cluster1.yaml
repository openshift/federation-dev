apiVersion: apps.open-cluster-management.io/v1
kind: Subscription
metadata:
  name: mongo-cluster1-app-subscription
  labels:
    app: mongo-cluster1-app
  annotations:
    apps.open-cluster-management.io/git-path: mongo/overlays/cluster1
spec:
  channel: demoNS/autodemo-gitops-gogs
  placement:
    placementRef:
      kind: PlacementRule
      name: cluster1
